# Development environment variables
# If you run json-server locally, set this to the local server. If you rely on the Vercel serverless API, leave it unset.

# Optional: set to http://localhost:4000 when running the json-server locally
VITE_API_URL=http://localhost:4000

